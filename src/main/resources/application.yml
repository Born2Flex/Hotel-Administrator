spring:
    application:
        name: gfl_final
    datasource:
        username: 'postgres'
        password: 'password'
        url: 'jdbc:postgresql://localhost:5432/hotel'
    jpa:
        open-in-view: false
        generate-ddl: true
        show-sql: true
        hibernate:
            ddl-auto: create-drop
